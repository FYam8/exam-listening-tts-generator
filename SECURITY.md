# Security Policy

APIキーや認証情報をこのリポジトリへ保存しないでください。本ツールの既定方式 `edge-tts` はAPIキーを使用しません。

誤って秘密情報、個人情報、非公開教材、権利上公開できない教材をコミットした場合、単なる削除コミットでは履歴に残ります。公開を止め、GitHubの公式手順に従って履歴から除去し、漏えいした認証情報は直ちに失効・再発行してください。

脆弱性を見つけた場合は、公開Issueへ秘密情報や再現用の私的台本を貼らず、リポジトリ所有者が用意した非公開の連絡手段を利用してください。

## WebアプリのローカルJSON

`web/` のファイル読込はFile APIで端末内のJSONを読み取ります。本アプリ自身には、そのJSONをサーバーへ送信するコードはありません。

ただしブラウザのWeb Speech APIは実装・音声によってオンラインサービスを使う場合があります。機密情報や公開できない個人情報は読み上げテキストに含めないでください。

学習履歴はLocalStorageに保存されます。氏名・メールアドレス等の個人情報は収集しません。

